modId = "endlessdimensions"
modName = "Endless Dimensions"
version = "1.0.0"
description = "Replicates 20w14infinite snapshot mechanics with unlimited block possibilities"
author = "EndlessDimensions"
minecraftVersion = "1.20.1"
entryPoint = "entry.js"

[server]
allowJavaInterop = true
hostAccess = true

[scripting]
allowJavaInterop = true
hostAccess = true
sandbox = false

[plugins]
# Bridge plugins are now loaded via minestom-ce-extensions system
# Extensions should be placed in the extensions/ directory
extensionsDir = "extensions"
# The extension system will automatically discover and load extensions
# No need to manually specify JAR files anymore
